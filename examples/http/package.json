{
  "name": "@mockdog/example-http",
  "version": "0.0.0",
  "description": "MockDog HTTP Example",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "build": "rm -rf dist tsconfig.build.tsbuildinfo && tsc --build --force tsconfig.build.json"
  },
  "private": true,
  "author": "Vitor Hugo Salgado <vsalgadopb@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@mockdog/http": "^0.0.0",
    "dotenv": "^16.0.2",
    "drizzle-http": "^3.1.0",
    "fastify": "^4.6.0",
    "pino": "^8.6.0",
    "pino-pretty": "^9.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
